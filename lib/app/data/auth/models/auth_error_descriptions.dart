abstract class AuthErrorDescriptions {
  const AuthErrorDescriptions._();

  static const weakPassword = 'The password provided is too weak.';
  static const emailAlreadyExists =
      'The account already exists for that email.';
}
